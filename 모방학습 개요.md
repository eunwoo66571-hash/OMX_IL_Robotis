# 모방학습 (Imitation Learning) 개요

## 1. 정의
모방학습(Imitation Learning)은 전문가(Expert)의 행동 데이터를 학습하여, 그 행동을 모방하도록 모델을 훈련시키는 머신러닝 기법이다.  
주로 강화학습(Reinforcement Learning)의 대안 또는 보완 방법으로 사용된다.

---

## 2. 핵심 아이디어
- 사람이 수행한 행동(데이터)을 그대로 학습
- 상태(State) → 행동(Action) 매핑을 학습
- 보상 함수를 직접 설계하지 않아도 됨

---

## 3. 주요 방식

### 3.1 행동 복제 (Behavioral Cloning)
- 지도학습 방식
- 입력: 상태(State)
- 출력: 행동(Action)
- 단순하고 구현이 쉬움

### 3.2 역강화학습 (Inverse Reinforcement Learning)
- 전문가의 행동을 보고 보상 함수 추정
- 이후 강화학습으로 정책 학습

### 3.3 DAgger (Dataset Aggregation)
- 모델이 예측한 행동을 전문가가 수정
- 데이터를 점진적으로 보완

---

## 4. 장점
- 보상 함수 설계가 필요 없음
- 학습 속도가 빠름
- 실제 데이터 기반으로 안정적인 학습 가능

---

## 5. 단점
- 전문가 데이터에 크게 의존
- 데이터 분포 밖 상황에 취약 (Distribution Shift)
- 오류가 누적될 가능성 있음

---

## 6. 활용 분야
- 자율주행 (운전 데이터 학습)
- 로봇 제어
- 게임 AI
- 인간 행동 분석

---

## 7. 강화학습과 비교

| 항목 | 모방학습 | 강화학습 |
|------|--------|--------|
| 학습 방식 | 전문가 데이터 기반 | 보상 기반 |
| 보상 함수 | 필요 없음 | 필요 |
| 데이터 요구 | 높음 | 낮음 |
| 탐색(Exploration) | 없음 | 있음 |

---

## 8. 간단한 예시 코드 (Python)

```python
# 간단한 행동 복제 예시
from sklearn.linear_model import LogisticRegression

# 상태 데이터 (예시)
X = [[0, 1], [1, 0], [1, 1]]
# 행동 데이터 (전문가)
y = [0, 1, 1]

model = LogisticRegression()
model.fit(X, y)

# 예측
print(model.predict([[0, 1]]))
